# WRITE-DEBUG-DECODE-AI-AGENT


Run pip install -r requirements.txt to add all the requirements

add a .env file and mention the GROQ_API_KEY to run the code

The prompt for a particular program generation is predefined in this code, to test for a different prompt, execute the command
python agent.py "Code for a number that is a happy number and an armstrong number as well" in the powershell
